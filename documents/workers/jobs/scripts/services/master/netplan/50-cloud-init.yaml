# This file is generated from information provided by
#
#the datasource.  Changes to it will not persist across an instance.
# To disable cloud-init's network configuration capabilities, write a file
# /etc/cloud/cloud.cfg.d/99-disable-network-config.cfg with the following:
# network: {config: disabled}
network:
  version: 2
  renderer: networkd
  ethernets:
    eno2:
      addresses: [ 10.40.23.250/21 ]
      nameservers:
          search: [ urumqi.farm.alt-chain.io ]
          addresses: [ 10.40.23.250 ]
      routes:
          - to: 0.0.0.0/0
            via : 10.40.23.254
            on-link: true
    eno1:
        dhcp4: no
        dhcp6: no

        #  vlans:
        #    vlan30:
        #        dhcp4: no
        #        dhcp6: no
        #        id: 30
        #        link: eno2
        #        addresses:
        #            - 10.0.0.1/16
        #        routes:
        #            - to: 10.0.0.0/16
        #              #via: 172.31.255.1 
        #              via: 0.0.0.0
        #              on-link: true
        #    vlan40:
        #        dhcp4: no
        #        dhcp6: no
        #        id: 40
        #        link: eno2
        #        addresses:
        #            - 10.10.1.100/24
        #        routes:
        #            - to: 10.10.1.0/24
        #              via: 0.0.0.0
        #              on-link: true