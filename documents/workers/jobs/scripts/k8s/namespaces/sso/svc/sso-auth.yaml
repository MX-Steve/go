apiVersion: v1
kind: Service
metadata:
  labels:
    k8s-app: sso-auth
  name: sso-auth
  namespace: sso
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 4180
  selector:
    k8s-app: sso-auth
  type: ClusterIP