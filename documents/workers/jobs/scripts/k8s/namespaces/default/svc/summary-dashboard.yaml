apiVersion: v1
kind: Service
metadata:
  labels:
    app: summary-dashboard
  name: summary-dashboard
  namespace: default
spec:
  externalTrafficPolicy: Cluster
  ports:
  - nodePort: 30046
    port: 80
    protocol: TCP
    targetPort: 10001
  selector:
    app: summary-dashboard
  sessionAffinity: None
  type: NodePort
